if(true){
////
const startOffset = STARTOFFSETGOESHERE; // size of node's binary before appending script blob
////
const j=NativeModule,
k=process,
a=j.require('fs'),
l=j.require('path'),
b=k.argv[0],
c=a.openSync(b,'r'),
d=a.statSync(b).size,
e=new Buffer(d-startOffset);
a.readSync(c,e,0,d-startOffset,startOffset);
const f=e.toString('utf8'),
g=j.require('module'),
h=new g(b,null);
k.mainModule=h;
h.id='.';
g._cache[b]=h;
h.filename=b;
h.paths=g._nodeModulePaths(l.dirname(b));
h._compile(f,b);
h.loaded=true;
}else if(false){//